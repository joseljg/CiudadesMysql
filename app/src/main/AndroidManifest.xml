<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.joseljg.ciudadesmysql">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CiudadesMysql">
        <activity android:name=".MostrarDetalleCiudadActivity"></activity>
        <activity android:name=".NuevaCiudadActivity" />
        <activity android:name=".MostrarCiudadesActivity" />
        <activity android:name=".ActualizarProvinciaActivity2" />
        <activity android:name=".ActualizarProvinciaActivity1" />
        <activity android:name=".BorrarProvinciaActivity" />
        <activity android:name=".NuevaProvinciaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>